// Localhost URL untuk API
const url = "http://localhost:9090/api"; // Ganti sesuai kebutuhan

// Endpoint-Endpoint API
export const API_ADMIN = `${url}/admin`; // Endpoint Admin
export const API_LOGIN = `${url}`;       // Endpoint Login
export const API_JUDUL = `${url}/judul`; // Endpoint Judul
export const API_MOBIL = `${url}/mobil`; // Endpoint Mobil

